name: Build

on:
  push:
    branches: [master]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            ext: ''
            archive_ext: tar.gz
          - os: ubuntu-latest
            ext: ''
            archive_ext: tar.gz
          - os: windows-latest
            ext: .exe
            archive_ext: zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka

      - name: Build (POSIX)
        if: matrix.os != 'windows-latest'
        run: |
          python -m nuitka \
            --standalone \
            --disable-ccache \
            --jobs=$(nproc 2>/dev/null || echo 2) \
            --output-filename=zentao-cli \
            zentao_api/cli.py
          # Tar up the standalone directory for distribution
          cd cli.dist
          tar czf ../zentao-cli-${{ runner.os }}-${{ runner.arch }}.tar.gz zentao-cli
          cd ..
          ls -la zentao-cli-${{ runner.os }}-${{ runner.arch }}.tar.gz

      - name: Setup MSVC env (Windows)
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install MinGW (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        continue-on-error: true
        run: |
          # GitHub windows-latest ships VS 2022 but NOT the C++ build tools
          # workload. MinGW-w64 is faster to install and Nuitka supports it
          # via --mingw-mode. ~30 sec download.
          choco install -y mingw-w64 --no-progress
          Write-Host "::add-path::C:\ProgramData\chocolatey\lib\mingw-w64\tools\mingw64\bin"
          gcc --version

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        continue-on-error: true
        shell: pwsh
        run: |
          python -m nuitka --standalone --disable-ccache --mingw-mode --output-filename=zentao-cli zentao_api/cli.py
          Compress-Archive -Path cli.dist/zentao-cli -DestinationPath zentao-cli-windows-x86_64.zip -Force
          Get-Item zentao-cli-windows-x86_64.zip

      - name: Smoke test (POSIX)
        if: matrix.os != 'windows-latest'
        run: |
          ./cli.dist/zentao-cli --help
          echo "---"
          ./cli.dist/zentao-cli 2>&1 | head -5 || true

      - name: Smoke test (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          .\cli.dist\zentao-cli.exe --help
          Write-Host "---"
          .\cli.dist\zentao-cli.exe 2>&1 | Select-Object -First 5

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zentao-cli-${{ runner.os }}-${{ runner.arch }}.${{ matrix.archive_ext }}
          path: zentao-cli-${{ runner.os }}-${{ runner.arch }}.${{ matrix.archive_ext }}
          if-no-files-found: error

  release:
    name: Attach binaries to GitHub Release
    needs: build
    if: always() && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
          fail_on_unmatched_files: false