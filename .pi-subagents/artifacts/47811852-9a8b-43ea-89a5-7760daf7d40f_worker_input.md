# Task for worker

You are a delegated subagent running from a fork of the parent session. Treat the inherited conversation as reference-only context, not a live thread to continue. Do not continue or answer prior messages as if they are waiting for a reply. Your sole job is to execute the task below and return a focused result for that task using your tools.

Task:
## 任务：P1 — 把 `zentao_api/client.py` 拆成 `zentao_api/client/` 包（mixin 组合）

### 上下文
仓库：`/Users/floating/workspace/zentao-api-skill/`
- `zentao_api/client.py`：3567 行，单类 `ZenTaoClient`，137 个方法
- 已有 P0+P4 的 mock 测试 `tests/test_p0_methods.py` (16) 和 `tests/test_p4_create_story.py` (3)，都通过
- `zentao_api/cli.py`：`from zentao_api.client import ZenTaoClient, read_credentials`，调用 client 的方法

### 目标
将 `client.py` 拆成 `client/` 包，按资源分模块；`ZenTaoClient` 仍是单一公共类（向后兼容 `cli.py` 的导入），但类体由 mixin 组合而成。

### 模块规划（每个一个 mixin 文件）

```
zentao_api/client/
├── __init__.py        # ZenTaoClient (composed) + read_credentials
├── _base.py           # BaseClient: __init__, session, get_session, old_request, clear_session
├── products.py        # ProductsMixin: get_products, get_product_list_old, get_product, create_product, edit_product, close_product, delete_product + P0 补的 get_projects/get_productplans/create_productplan
├── projects.py        # ProjectsMixin: get_project_list_old, get_project_tasks_old, get_project, create_project, start_project, close_project, edit_project, get_project_stories, get_project_bugs, manage_project_members, link_project_story, unlink_project_story, get_project_team, get_project_dynamic + P0 补的 get_executions
├── stories.py         # StoriesMixin: get_story, create_story, edit_story, close_story, activate_story, delete_story, change_story, review_story, get_story_tasks, get_story_bugs, get_story_cases, link_story_project, unlink_story_project + P0 补的 get_stories
├── tasks.py           # TasksMixin: delete_task, cancel_task, close_task, start_task, finish_task, pause_task, restart_task, activate_task, assign_task, create_task, create_tasks, create_subtasks, get_task_detail, edit_task, move_task, copy_task, get_task_subtasks, link_task_story, link_task_bug, get_task_history, record_estimate, get_estimate, edit_estimate, delete_estimate + P0 补的 get_tasks/batch_create_tasks
├── bugs.py            # BugsMixin: get_bug, create_bug, create_bug_from_testcase, resolve_bug, close_bug, activate_bug, assign_bug, confirm_bug, delete_bug, edit_bug, link_bug_story, unlink_bug_story, link_bug_task, unlink_bug_task, get_bug_statistics, add_bug_comment, get_project_bugs + P0 补的 get_bugs
├── qa.py              # QAMixin: get_testcases, get_testcase, create_testcase, delete_testcase, edit_testcase, batch_create_testcases, import_testcases, export_testcases, link_testcase_story, unlink_testcase_story, get_testsuites, get_testsuite, create_testsuite, delete_testsuite, get_testtasks, get_testtask, create_testtask, delete_testtask, start_testtask, close_testtask, block_testtask, activate_testtask, get_testreports, delete_testreport
├── releases.py        # ReleasesMixin: get_releases, create_release, edit_release, get_release, delete_release, link_release_story, unlink_release_story, link_release_bug, unlink_release_bug
├── builds.py          # BuildsMixin: create_build, edit_build, get_build, delete_build, link_build_story, unlink_build_story, link_build_bug, unlink_build_bug
├── plans.py           # PlansMixin: get_plans, create_plan, delete_plan, edit_plan, get_plan, link_plan_story, unlink_plan_story, get_productplan_list_old
└── mine.py            # MineMixin: get_my_tasks, get_my_bugs, get_my_stories, get_my_projects
```

`__init__.py` 用 mixin 组合：
```python
class ZenTaoClient(BaseClient, ProductsMixin, ProjectsMixin, StoriesMixin, TasksMixin, BugsMixin, QAMixin, ReleasesMixin, BuildsMixin, PlansMixin, MineMixin):
    """禅道 API 客户端（向后兼容门面）"""
```

`read_credentials` 放在 `_base.py` 或 `__init__.py`（保持导出位置）。

### 强制要求
1. **禁止使用 subagent 工具**。不要 dispatch subagent，不要用 `mcp` 工具，不要用 `intercom`。只用 `read`、`edit`、`write`、`bash`。任何时候发现自己想 dispatch，立刻停下来自己做。
2. **行为零变化**：所有方法签名、返回值、内部逻辑保持原样。只搬代码，不重写。
3. **保留 `# === xxx ===` 分块注释**作为每个 mixin 的开头注释，方便后续维护。
4. **完整保留所有 P0 新增方法和第一阶段 commit 的所有改动**。
5. **`cli.py` 不需要改**：继续 `from zentao_api.client import ZenTaoClient, read_credentials`，包导入与模块导入语法一致。

### 执行步骤
1. 先用 `wc -l` 和 `grep -n "^    def "` 摸清原文件结构和每个方法所在行号
2. 按行号从 `client.py` 读取每个方法块（含 docstring），整段搬到对应 mixin 文件
3. 写 `client/_base.py`（最优先：所有 mixin 依赖 BaseClient 的 `old_request`、`self.sid`、`self.session` 等）
4. 写所有 10 个 mixin 文件
5. 写 `client/__init__.py`：import 所有 mixin + BaseClient + read_credentials，组合 `ZenTaoClient`
6. **删除 `client.py`**（用 `git rm zentao_api/client.py`），让包 `client/` 接管这个命名空间
7. 跑测试：`cd /Users/floating/workspace/zentao-api-skill && .venv/bin/python -m pytest tests/ -v`，必须全过
8. 跑 import smoke test：`cd /Users/floating/workspace/zentao-api-skill && .venv/bin/python -c "from zentao_api.client import ZenTaoClient, read_credentials; c = ZenTaoClient('http://x', 'u', 'p'); print(len([m for m in dir(c) if not m.startswith('_')]))"`，应输出非零（应有 100+ 方法）

### 提交流程
完成后一次性 commit：
```
git add -A zentao_api/
git commit -m "P1: 拆 client.py 为 client/ 包（10 个 mixin + base），向后兼容 ZenTaoClient 门面"
```

### 不要做
- 不要重命名方法
- 不要删除任何方法（即使有重复或看起来没用，保留）
- 不要修 bug（即使看到 bug，留给后续 P2/P4）
- 不要加新方法
- 不要改 cli.py
- 不要碰 scripts/test_*.py（真服务器测试）
- 不要碰 .venv/、build/、.memsearch/、.pi-subagents/

### 完成后报告
- 新增的文件清单 + 每个文件行数
- 删除的 `zentao_api/client.py` 行数（应接近 3567，但减少是因为拆出去了，模块总和应略大于 3567）
- pytest 输出（全部 PASS）
- import smoke test 输出
- commit hash
- 任何意外发现（例如方法跨多个 mixin 边界、命名冲突）

## Acceptance Contract
Acceptance level: reviewed
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Implement the requested change without widening scope
- criterion-2: Return evidence sufficient for an independent acceptance review

Required evidence: changed-files, tests-added, commands-run, validation-output, residual-risks, no-staged-files

Review gate: required by reviewer.

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
`criteriaSatisfied[].status` must be exactly one of: satisfied, not-satisfied, not-applicable.
`commandsRun[].result` must be exactly one of: passed, failed, not-run.
`manualNotes` and `notes` are optional strings; an empty string means no note and does not satisfy `manual-notes` evidence.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    },
    {
      "id": "criterion-2",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```